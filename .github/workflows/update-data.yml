name: Aktualizace podnikatelské mapy

on:
  push:
    branches: [main]
    paths-ignore:
      - 'data/firms.json'
  workflow_dispatch:
  schedule:
    - cron: '20 6 2,17 * *'

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Dependencies
        run: pip install pyproj
      - name: Download RES
        run: |
          curl -L --fail --retry 6 --retry-delay 10 --retry-all-errors --connect-timeout 30 --max-time 1200 \
            -A 'podnikatelska-mapa-lomnice/1.0' \
            'https://opendata.csu.gov.cz/soubory/od/od_org03/res_data.csv' \
            -o res_data.csv
          test -s res_data.csv
          ls -lh res_data.csv
      - name: Build data
        run: python scripts/build_data.py
      - name: Commit generated data
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add data/firms.json
          git diff --cached --quiet || git commit -m 'Update RES/RUIAN business data'
          git push
